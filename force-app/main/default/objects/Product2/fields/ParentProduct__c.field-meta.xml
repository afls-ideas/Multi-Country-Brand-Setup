<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ParentProduct__c</fullName>
    <label>Parent Product</label>
    <description>Custom lookup to parent Product2 record. Use this field when the standard Product Hierarchy feature (ParentId) is not enabled. Provides Brand → Sub-Brand → Sample relationships.</description>
    <inlineHelpText>Select the parent product. Brands have no parent. Sub-Brands point to their Brand. Samples point to their Sub-Brand.</inlineHelpText>
    <type>Lookup</type>
    <referenceTo>Product2</referenceTo>
    <relationshipLabel>Child Products</relationshipLabel>
    <relationshipName>ChildProducts</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <deleteConstraint>SetNull</deleteConstraint>
</CustomField>
